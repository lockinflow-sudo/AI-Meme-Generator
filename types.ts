
export interface MemeTemplate {
  name: string;
  url: string;
}

export interface ImageData {
  base64: string;
  mimeType: string;
  url: string;
}

export interface MemeFont {
  name: string;
  fontFamily: string;
}
